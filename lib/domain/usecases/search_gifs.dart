/// Placeholder — exemplo de caso futuro.
/// Aqui entraria o caso de uso para buscar GIFs por palavra‑chave.
class SearchGifs {
  // Exemplo: final GifRepository repository;
  // SearchGifs(this.repository);

  // Future<List<GifEntity>> call(String query) async {
  //   return repository.searchGifs(query);
  // }
}